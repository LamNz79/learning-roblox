--!strict
local EnemyConfig = {
	["BasicEnemy"] = {
		Speed = 10, -- Seconds to cross the map
		Health = 100,
		Damage = 1, -- How much base health it takes
	},
	-- You can add more later!
}

return EnemyConfig
